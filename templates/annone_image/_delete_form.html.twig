<form method="post" action="{{ path('app_annone_image_delete', {'id': annone_image.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annone_image.id) }}">
    <button class="btn">Delete</button>
</form>
